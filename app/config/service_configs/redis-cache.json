{
  "defaults": {
    "selections": {
      "skuName": "C0",
      "productName": "Azure Redis Cache Basic",
      "armRegionName": "chinaeast2"
    },
    "hours_per_month": 730
  },
  "service_name": "Azure Cache for Redis",
  "quantity_label": "实例数",
  "quantity_model": "instances_x_hours",
  "api_service_name": "Redis Cache",
  "dimension_labels": {
    "skuName": "大小",
    "productName": "层级"
  },
  "quantity_formula": {
    "inputs": [
      {
        "key": "shards",
        "min": 1,
        "label": "Shard per Instance",
        "default": 1
      },
      {
        "key": "additional_replicas",
        "min": 0,
        "label": "Additional Replicas per Shard",
        "default": 0
      },
      {
        "key": "instances",
        "min": 1,
        "label": "Instance",
        "default": 1
      }
    ],
    "formula": "shards * (1 + 1 + additional_replicas) * instances",
    "use_meter": "Cache Instance",
    "applies_to": {
      "productName": [
        "Azure Redis Cache Premium"
      ]
    },
    "display_steps": [
      "{shards} Shard x (1 Primary + 1 Built-in + {additional_replicas} Additional) = {nodes} Nodes",
      "{nodes} Nodes x {instances} Instances x {hours} Hours x {fmtPrice}/hr = {fmtTotal}"
    ],
    "intermediate_vars": {
      "nodes": "shards * (1 + 1 + additional_replicas)"
    }
  },
  "_legacy_reference": {
    "slug": "redis-cache",
    "display_name_cn": "Azure Redis 缓存",
    "pricing_pattern": "A",
    "pricing_pattern_name": "instances_x_hours"
  },
  "hidden_dimensions": []
}
